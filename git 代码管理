1 每次修改代码后先查看状态   git status 

2 提交代码 git commit -m "新增xxx功能" 

3 将代码提交远程服务器(github) git push 
  会提示输入用户名和密码
  
  ---------------------------------------
  1  git remote -v 查看远程仓库信息 
  
  2 git remote ，只是会单纯的列出所有远程仓库的名字，不会展示远程仓库的地址
  
  3 将远程版本库中的最新代码同步到本地
    a git fetch origin master  只下载，不合并
    b git pull origin master   下载合并同时进行
  
  4  将修改后的代码同步到远程版本库
    git push origin master
   
   
  5 添加远程仓库
  git remote add [shortname] [url]
  
  6 查看远程仓库信息
  git remote show [shortname]
  
  7 重命名远程仓库
  git remote rename [old-name] [new-name] 
  
  8 删除远程仓库（解除与远程仓库的关系）
  git remote rm [shortname] 
  
  
